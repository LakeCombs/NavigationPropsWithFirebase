// export const AppReducer = (state, action) => {
// 	switch (action.type) {
// 		case "ADD_VALUE":
// 			action: return {
// 				...state,
// 				students: [action.payload, ...state.students]
// 			};

// 		case "DELETING_VALUE":
// 			action: return {
// 				...state,
// 				student: state.student.filter(id => {
// 					return name.id !== action.payload;
// 				})
// 			};

//             // case "EDITING_VALUE":
            

// 		default:
// 			return state;
// 	}
// };
